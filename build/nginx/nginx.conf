events {
    worker_connections 1024;
    }

http{
    upstream resize-cluster {
        server service1:8090;
        server service2:8090;
    }

    server {
        listen 8090;
        server_name localhost;
        location / {
            proxy_pass http://resize-cluster;
            proxy_set_header Host $host;
        }
    }
}